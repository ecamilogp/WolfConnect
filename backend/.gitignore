node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

skills-lock.json

# Local attachment storage (Multer writes here at runtime; never commit uploaded files)
/uploads
