PORT=
NODE_ENV=
JWT_SECRET=
DATABASE_URL=

# Comma-separated list of emails to promote to platform ADMIN when running
# `npm run db:seed`. The accounts must already be registered. This is the
# only supported way to create the first administrator.
PLATFORM_ADMIN_EMAILS=


SOCKET_CORS_ORIGIN=

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
FRONTEND_URL=
